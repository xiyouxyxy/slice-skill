---
name: slice
description: 切片计划 / goal slicer — 把大目标、课程提纲或长期任务拆成每天可执行的微小计划。当用户想把"大东西"切成"每天做的小事"时使用，典型句式："帮我把学 Python 切成每天计划""把这门课的提纲排进日程（X 月 X 日前学完）""制定一个减肥 / 备考 / 写书的每日计划""把 X 拆成每日任务 / 切片 / 每天安排一点"。支持可选每日时长、截止日反推、课程型间隔复习、每日打卡自动化与落后自动重排。不限学习场景，长期项目、习惯养成、备考等通用。
---

# Slice — 切片计划

## Overview

把大目标、课程提纲或长期任务切成每天可执行的微小计划，并支持每日打卡、间隔复习与落后自动重排，帮助计划真正落地、不拖延。

## When to use

用户想把一件"大东西"拆成"每天做的小事"时触发，典型句式：

- "帮我把学 Python 切成每天计划"
- "把这门课的提纲排进日程 / 到 X 月 X 日前学完"
- "制定一个减肥 / 备考 / 写书的每日计划"
- "把 X 拆成每日任务 / 切片 / 每天安排一点"

不限学习场景，长期项目、习惯养成、备考等通用。

## 核心概念：目标卡片

所有输入收敛为一张目标卡片（不必一次问全，按需追问）：

- 目标描述（必填）
- 类型：学习 / 项目 / 习惯（影响默认节奏与复习策略）
- 截止日（可选）
- 每日时长预算（可选；可区分工作日 / 周末）
- 可用日（可选，如"仅工作日""周末多安排"）
- 优先级（可选）

## 工作流

### 0. 入口前置条件问询（项目型必问 / 学习·习惯型可选）

在拆解之前，先把"动手前必须备齐的资源"问清楚——这类信息（账号 / 权限 / 数据 / 素材 / 特定软件）只有用户自己知道，LLM 推不出来，漏了会在中后期卡死。

1. **先判定目标类型**（学习 / 项目 / 习惯；模糊时用一句话确认，如"这是学习类还是做项目？"）。
2. **按类型决定是否问前置条件**：
   - **项目型：必问。** 抛给用户的**开放式**问题（**不要替用户预设清单**，让他自己列）：
     > 完成这个目标，你手上已经有哪些**工具 / 账号 / 权限 / 数据 / 素材**？还缺什么？
     > （不知道可以跳过，我会在排里程碑时按需补。）
   - **学习 / 习惯型：可选。** 仅在用户明显有已知门槛时顺带问一句，否则跳过：
     > 如有已知的工具门槛（如需要装某软件 / 某账号），现在可告诉我；没有就跳过，我会在相关里程碑里列。
3. **落档**：把用户给的清单写入计划文件的 `## 前置条件` 节（见 `references/plan-template.md`）。
4. **可留空**：用户说"不知道 / 跳过"时，前置条件节写「未填写——LLM 在排里程碑时按需在相关里程碑头部补 `前置：…`」。
5. **计划可改**：前置条件不是一次定死。用户后续随时可补充 / 修改（或说"我还缺 X"），LLM 据此回写前置条件节，并调整前几个 setup 里程碑（见步骤 9）。

> 注意：本步骤**只问、不推断**——不替用户列"通常需要什么"，完全按用户报的内容记录；用户没给的，留到追问或 WBS 时再补。

### 1. 解析意图与模式

判断模式：

- **目标型**：用户给的是"一个目标 / 任务"，无现成提纲。
- **课程型**：用户给的是"提纲 / 章节列表 + 预计完成时间"。

### 2. 澄清与量化（仅必要时追问）

- 若目标模糊（如"变健康""学好英语"），先问 1–2 个关键问题使其可度量：
  - "怎样算完成？有没有可检验的产出或里程碑？"
  - "当前起点在哪？零基础还是已有基础？"
- 若用户已给提纲 / 产出，跳过追问。
- 已锁定决策：当用户**不输入每日时长且无截止日**时，直接给默认节奏方案，**不再追问**（见步骤 5）。

### 3. 工作分解（WBS）+ 完成标准三件套

把目标拆成 3–6 个里程碑，每个里程碑再拆成 25–50 分钟原子任务。**每个原子任务除了标题外，必须同时定义「完成标准三件套」**，否则不算合格的切片：

**先消化 `## 前置条件` 节**（步骤 0 的产出，可能为空）：
- 缺的项 → 排成前 1–3 个 **setup 里程碑**（M0/M1），如"装环境 / 注册账号 / 申请权限 / 下数据集"，打通工具再进内容。
- 已齐的项 → WBS 时不再重复列。
- 行政 / 治理类长周期依赖（如"等采购批文""走 IT 流程"）→ 第 1 周就发起、最后一周验收，绝不拖到后期才暴露。
- 学习 / 习惯型若当时跳过了前置问询 → 在相关里程碑头部按需补一行 `前置：…`（如"前置：pandas 已装、数据集已下到 ./data/"），把隐藏门槛就地标出。

1. **子目标**（2–4 条可勾选的具体能力点）
   - 用「能……」「会……」「能写出……」开头；动词具体、程度明确。
   - 反例：「了解 X」「理解 Y」——不可勾选、不可验证。✅ 正例：「能区分可变/不可变类型（int/str/list/tuple）」「能写出 if/elif/else 嵌套」。

2. **验收**（用什么方式证明你真的会了）
   - **学习新概念类** → 3 道左右小测试题，给具体题目名（例：「变量交换」「FizzBuzz」「列表去重」）。
   - **编程类** → 1–3 道实操题，必须能在编辑器跑通。
   - **项目类** → 1 个可交付物（脚本名 / 数据集 / 报告段落 / commit hash）。
   - **习惯类** → 可量化的当日动作（步数 / 时长 / 次数）。

3. **口述自检**（Feynman 法）——**必须由 LLM 根据当天任务内容自动生成，不能让用户自己填，也不能写空泛的"口述今天学到的内容"**。每张卡片生成 **1–3 道**具体「讲清 / 对比 / 解释」题，命中当天**最易混、最难、最容易"以为懂其实不懂"**的点。
   - **生成配方（照做）**：
     1. 从当天任务的子目标里挑 1–3 个最容易混淆 / 最关键的点（如概念配对：可变 vs 不可变、continue vs break、浅拷贝 vs 深拷贝；或反直觉结论：为什么 X 而不是 Y）。
     2. 把每个点 phrasing 成一道"用自己的话讲清"的题，限定"不查资料、1–2 分钟内讲得清"。
     3. 优先用「对比 / 为什么 / 讲给小白」三类句式，逼出因果与原理，而非复述定义。
   - **句式模板**：「口述『X 与 Y 的区别（至少 2 点）』」「口述『为什么 X 而不是 Y』」「口述『X 的内部执行流程』」「口述『你会怎么向零基础朋友解释 X』」。
   - **质量门槛（否则重出）**：
     - ✅ 好：具体、指向当天内容、要求解释因果（"口述『列表 vs 元组的区别』""口述『为什么 for 循环里改列表长度会乱'")。
     - ❌ 差：空泛（"口述今天学到的内容""讲讲你的收获"）、Yes/No 题（"口述什么是变量"）、直接抄子目标原文、与验收题雷同。
   - 例：「口述『列表 vs 元组的区别（至少 2 点）』」「口述『continue vs break 的区别』」「口述『为什么字符串不可变却能用 + 拼接』」。
   - 这一步把"被动读"变成"主动想"，是抗遗忘与暴露盲区的核心动作。

**为什么是三件套而不是单件**：① 子目标把"模糊概念"变成"具体能力清单"；② 验收强迫主动输出而非被动输入；③ 口述自检（由 LLM 现生成、针对当天难点）能发现"以为懂其实不懂"的盲区。三者互补，缺一不可。

不同任务类型的三件套组合：

| 任务类型 | 子目标 | 验收 | 口述自检 |
|---------|--------|------|----------|
| 学习新概念 | ✓ 必填 | 题 1–3 道 | ✓ 必填 |
| 编程类 | ✓ 必填 | 实操题 1–3 道 | ✓ 推荐 |
| 项目类 | ✓ 必填 | 1 个交付物 | 可选 |
| 习惯类 | 动作项 | 时长 / 频次 | 简短复盘 |

课程型：以提纲章节 / 小节作为天然单元，直接映射为任务；子目标与口述自检按上表填充。

### 4. 估算总工时（含验收时间）

- 对每个原子任务，工时 = 「学习/操作时长」+ 「验收时长」。
- 学习/操作时长：粗略给 0.5h / 1h 等。
- 验收时长：每道测试题 0.1–0.15h，口述 0.1h。
- 例：「M1: Python 语法回顾」= 操作 0.7h + 验收 0.2h（3 道题）+ 口述 0.1h ≈ 1.0h。
- 若用户已给总时长，以用户值为准并按 70% / 30% 拆分学习与验收。
- 求和得总工时 T，进入步骤 5。

### 5. 排程（双向推算 + 默认节奏）

全部基于"可用日"与"每日时长预算"。按以下优先级：

- **有截止日**：把总工时 T 摊到截止日前的可用日，每日负荷 = T / 可用天数。若单日负荷超过用户时长预算或 > 2.5h，提示"时间紧"，建议延长截止日或提高每日投入。
- **有每日时长预算（无截止日）**：完成日 = 今天 + ceil(T / 每日时长) 个可用日，正推得出。
- **两者都无（已锁定：默认节奏，不追问）**：采用默认节奏——
  - 学习 / 习惯：每周 5 个可用日、每天 1 个原子任务（约 25–50min）。
  - 项目：每周 5 日、每天 1–2 个原子任务。
  - 据此给出预计完成日，并明确告知："这是默认节奏，你可以随时告诉我每天能投入多久，或想在哪天前完成。"

### 6. 插入复习与缓冲

- **课程型 / 学习**：对每段新内容，在学完后的 +1 / +3 / +7 天插入复习任务（复习计入当日时长，可占用缓冲）。
- **缓冲日**：每 5 个学习 / 工作日后插入 1 个缓冲日（无新内容，用于补漏或休息）。
- **最小可执行单元**：即使某天很忙，也保留一个 ≤10 分钟的微任务（如"看一页""复习 3 个闪卡"），保证连续不断。

### 7. 持久化计划文件

在当前 workspace 下写入 `.slice/plans/<goal-slug>.md`，格式见 `references/plan-template.md`。若同目标已存在计划，先读旧计划再覆盖 / 续接。文件包含：元信息（目标卡片）、总览（里程碑 / 总工时 / 预计完成日）、每日计划表、进度区（留空待打卡更新）。

### 8. 每日打卡提醒（可选）

- 可选：配置一个每日提醒（如 Claude Code 的 `/cron`、系统定时任务，或你所用 agent 提供的定时能力），在用户偏好时间（默认 21:00）发送"今日打卡"提示，内容为当天任务清单 + "完成了吗？/ 今天实际投入多久？"。
- 打卡逻辑固定为：读取 `.slice/plans/<goal-slug>.md`，输出今天任务，询问完成情况，必要时触发步骤 9 的重排。
- 若环境无定时能力或用户不想用，跳过本步，改为按需取当日计划。

### 9. 每日打卡与自适应（落后自动重排）

每次打卡后：

- 标记当日完成度（基于三件套勾选状态）：
  - `⬜` 待做
  - `🟡` 部分（三件套未全勾，至少子目标 + 验收有一个勾上）
  - `✅` 全过（**三件套全勾** = 子目标全勾 + 验收完成 + 口述完成）
  - `⏭` 跳过
- **部分完成的进度换算**：`🟡` 算 0.5 个任务单位（避免连续 🟡 拖垮节奏）；但子目标 + 验收都完成而口述未做时，`🟡` 算 0.8（口述最易补上）。
- **落后**：剩余任务按"缓冲日先行消化 → 顺延 → 压缩单日负荷（上限 2.5h）"重排；若落后超过总进度的 30%，提示用户延长截止日或提高投入，不要默默硬塞。
- **领先**：把后续任务前移或增加深度（如提前进入下一章、加做练习、子目标加一条"能解释为什么 X 而不是 Y"）。
- 回写计划文件进度区与每日表，并把对应任务卡片的三件套勾选状态同步更新。
- **前置条件变更**：用户中途说"我还缺 X"或补充任何资源 / 权限 / 数据 → 回写 `## 前置条件` 节并更新状态行，必要时把缺的项排成新的 setup 任务或顺延被它阻塞的里程碑；若阻塞已发生，按"落后"逻辑重排（见上）。

### 10. 导出（让用户选格式，别默认只给 .ics）

规划完成后，**主动询问用户想要哪种导出格式**，不要一股脑只生成 `.ics`（很多用户电脑上没有日历 App，打不开）：

- **Markdown（默认推荐）**：计划本身就是 `.md` 文件（步骤 7 已写入 `.slice/plans/<goal-slug>.md`），可直接用记事本 / VS Code / 任意文本编辑器打开，**无需额外导出**。
- **.ics 日历**：仅给想把任务导入手机/电脑日历（Apple 日历、Google 日历、Outlook）的人。用 `scripts/export_ics.py`：先生成 JSON 事件数组（每行为 `{"date":"YYYY-MM-DD","title":"...","desc":"..."}`），再运行脚本产出 `.ics`。
- **HTML（推荐给"打不开 .ics"的用户）**：生成一个排版好的独立网页，含每日勾选框、可打印，**双击即用浏览器打开，不需要任何额外软件**。用 `scripts/export_html.py`，参数与 `export_ics.py` 完全一致（`--in` 同款 JSON → `--out` `.html`）。
- **全部 / 多个**：用户想要几个就给几个。

询问话术示例：
> 计划已生成 ✅ 你想怎么导出？
> ① Markdown（直接看，默认） ② .ics 日历（导入手机日历） ③ HTML（浏览器打开、可勾选打印） ④ 全要
> 回复数字即可。

**默认不自动生成 `.ics` / `.html`**；只有用户选了才跑对应脚本，避免产出用户打不开的文件。

## 边界与兜底

- **不切实际截止日**：单日负荷 > 2.5h 或完成日早于"今天 + 总工时 / 上限"时，明确预警并给 1–2 个替代方案（延长截止日 / 提高每日投入 / 砍范围）。
- **多目标并行**：每个目标独立一个计划文件，互不干扰；打卡时列出所有进行中的今日任务。
- **模糊回报**：用户说"没做完"时，追问具体卡在哪、能否今天补，而不是直接判失败。

## Resources

- `references/plan-template.md`：计划文件标准格式与填写示例（含三件套卡片）。
- `references/examples.md`：目标型 / 课程型 / 长期任务型三个完整示例。
- `scripts/export_ics.py`：计划 → `.ics` 日历导出。
- `scripts/export_html.py`：计划 → 独立 HTML 勾选清单（浏览器打开，无需额外软件，三件套子项作为可勾选框输出）。
